# VictoryPVI 本地多端同步

实现方案：纯网页、离线 WebRTC DataChannel，2026-09-05。

## 使用边界

设备已打开网页后，同步不需要互联网、账号、Cloudflare 或本地服务进程。设备连接同一 Wi-Fi 或手机热点。浏览器通过 `RTCPeerConnection({ iceServers: [] })` 收集本地候选地址，不请求 STUN、TURN 或在线信令。页面不发出任何同步 HTTP / WebSocket 请求。

浏览器不能在局域网监听普通 HTTP/WebSocket 服务，也无法凭 6 位数字查找另一台设备。没有共享信令服务时，两端必须交换 offer 和 answer。新版明确保留这一次双向交换，移除无法建立连接的核对短码以及云服务配置入口。

## 连接流程

1. 操作端点“连接其他设备”，显示邀请二维码。
2. 跟随端点“扫码加入”，扫描邀请后立即显示回码。
3. 操作端点“扫描对方回码”；识别后自动建立连接，收起配对界面。

二维码直接携带本地连接信息，在已经打开的网页中扫描，不要求跳转在线网页。摄像头不可用时，在折叠区域复制完整连接码。默认使用 deflate 压缩降低二维码密度；“兼容连接码”切换为无压缩版本供旧浏览器读取。配对码带独立邀请 ID，避免把旧回码应用到新连接。

操作端可以点“添加 / 重连设备”逐台添加跟随端。每个邀请对应独立 PeerConnection；生成新邀请不会关闭现有设备。重新配对相同设备时，确认回码后替换它的旧连接。

## 状态与可靠性

- 一台操作端写入，多台跟随端只读。患者姓名、病案号和 PDF 仍不进入同步快照。
- 本地模式直接传递状态 JSON，不要求房间密钥、令牌或应用层加密。WebRTC 自身的传输机制由浏览器处理。
- 每次修改先写入本机，120 ms 合并连续变化，再发送带 revision 的完整快照。
- 始终保留最新完整快照。新设备连接及发送 hello 时立即补发，即使操作端此刻没有新操作。
- 大快照拆分为小消息，限制发送缓存；接收端完整组装并应用后才发送 ACK。发送动作本身不算同步成功。
- 未确认的最新快照每 3 秒重发；每台设备独立确认、独立发送，较慢设备不阻塞其他设备。
- 重复或较旧 revision 不会重复应用。中断中积累的多次修改合并为最新状态，清空和撤回也按完整快照恢复。
- 每 3 秒探测连接；20 秒未收到任何消息时显示断开，保留最后记录。网页返回前台或网络恢复时主动发送 hello 请求补齐。
- 连接信息收集超时会明确报错，不生成缺少候选地址的半成品邀请。

## 不能承诺的恢复

短暂丢包、切到后台后返回且原 WebRTC 会话仍有效时，可以自动恢复传输。刷新网页、关闭页面、更换网络、浏览器终止会话或底层 ICE 已彻底失败后，必须重新扫码交换连接信息。没有信令服务时，保存旧 SDP 不能可靠重建连接。

重新打开网页保留记录与设备角色，并明确显示“连接中断”。原跟随端继续只读，避免把过期镜像误当作操作端。用户可以主动退出同步再独立记录。

Wi-Fi 的设备隔离、热点不允许客户端互访或浏览器不支持 WebRTC 时，纯网页无法绕过限制，应切换可互访的网络或浏览器。网页首次加载仍需要获取静态页面及资源；“离线同步”指页面加载完成后的连接与状态传输。

## 验证

`node tests/sync-client-smoke.mjs` 覆盖：无在线传输入口、配对解析、多镜像、晚加入补发、大快照分块、ACK、丢包后的最新状态补发、清空、重复与旧 revision、心跳过期。

浏览器实测使用多个独立浏览器上下文，通过界面的完整码备用路径交换与二维码相同的内容，验证真实 WebRTC 连接、两个镜像、消融操作、撤回、刷新后的断开提示及只读状态。物理手机摄像头扫码与医院 Wi-Fi 的互访情况需在实际设备上验证。

`cloudflare/` 保留旧版服务源码供历史版本使用，当前网页不加载、不连接它。
