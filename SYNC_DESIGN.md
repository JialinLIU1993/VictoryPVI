# VictoryPVI 多端设备同步设计

文档状态：设计稿 v0.3 · 2026-09-04

实现状态：前端实时镜像与 Cloudflare Worker/ Durable Object 代码已落地，并已部署到目标 Cloudflare 账号；生产环境继续保留 HTTPS 备用同步通道。

## 1. 目标

让医生可以在办公室电脑、术中工作站和平板之间继续使用同一份肺静脉消融记录，同时保留当前“打开即用、默认本地保存、不上传患者信息”的体验。

同步只覆盖当前记录所需的质控数据，不同步患者基础信息和导出的 PDF：

- 肺静脉口部、前庭和脊部的消融计数；
- 消融序列、PVI 终点和最近操作；
- 本台术式设置与当前放电电极选择；
- 记录版本、设备标识和同步时间。

## 2. 推荐方案

采用“本地优先 + 端到端加密 + 可插拔服务”的结构。

1. 页面启动时仍然直接读取本机记录，断网也能完整记录。
2. 用户主动连接同步空间后，客户端生成同步密钥；服务端只保存密文和必要的版本索引。
3. 新设备通过一次性配对码或二维码加入同步空间，不要求把患者信息放进 URL、二维码或日志。
4. 每次消融操作先写本机，再进入待同步队列；网络恢复后自动重试。
5. 检测到并行修改时不静默覆盖，先展示差异，由用户明确选择或合并。

当前静态 GitHub Pages 版本没有后端，因此第一阶段可以先实现同步中心和加密同步包，作为可立即使用的跨设备迁移；第二阶段再接入云端 Provider，升级为自动同步。

## 2.1 配对后的实时同步行为

配对不是一次性复制，而是建立一个持续的同步会话：

- **操作端（主设备）**：负责记录消融、撤回、清空、修改终点和术式设置；每次状态变化立即生成一个带序号的操作事件。
- **配对设备（镜像端）**：收到事件后自动回放到本机状态，重新渲染肺静脉分布图、消融概览和终点状态，不需要手动刷新。
- **首次配对**：先发送一次完整加密快照，再持续发送增量事件；镜像端进入会话后显示“实时跟随”。
- **断线恢复**：中继服务记录加密事件游标；重新连线后先补齐遗漏事件，发现游标不连续时自动请求完整快照。
- **确认反馈**：操作端显示“已同步到 1 台设备”或“待同步 2 条”，镜像端显示“刚刚更新”，不把网络延迟伪装成已完成。

默认把配对设备设为只读镜像，适合术中一台设备操作、另一台设备查看。如果未来允许两端都编辑，则必须启用 revision 检查和冲突页面，禁止直接采用最后写入覆盖临床记录。

实时链路可以使用 WebSocket/Realtime 中继，也可以在同一局域网内使用 WebRTC DataChannel；跨网络、后台恢复和离线补发仍需要受控的同步服务。无后端时不能承诺跨设备自动跟随，只能提供加密同步包迁移。

事件信封示例：

```js
{
  type: "state-event",
  eventId: "uuid",
  recordId: "uuid",
  sequence: 43,
  baseRevision: 42,
  deviceId: "operator-device",
  occurredAt: "2026-09-04T14:32:10.000Z",
  operation: {
    kind: "ring-ablation",
    veinId: "LSPV",
    layerId: "outer",
    sector: 3,
    delta: 1
  }
}
```

## 3. 界面设计

### 3.1 顶部状态入口

在现有顶部保存状态旁增加一个轻量状态入口，不做大面积仪表盘：

```text
记录已自动保存   ● 仅本机       关于   术式设置   撤回   清空记录   导出 PDF
                  └─ 同步状态入口
```

状态只表达三件事：

- `仅本机`：尚未连接同步空间；
- `待同步 3 条`：本地有操作等待上传；
- `已同步 · 2 台设备`：当前记录已同步，显示最近同步时间。

颜色遵循现有系统：紫色表示可操作、绿色表示已同步、橙色表示待同步或需要处理，红色只用于失败或冲突。

### 3.2 同步中心

点击状态入口后打开与现有“术式设置”一致的对话框，内容按任务顺序排列：

```text
┌ 设备同步                                        × ┐
│ 当前记录                                             │
│ ● 仅本机保存                                           │
│ 本机记录不会自动离开此设备。连接后只同步质控数据。     │
│                                                       │
│ [连接新设备]   [导出加密同步包]   [导入同步包]          │
│                                                       │
│ 同步空间：未连接                                      │
│ 设备：本机（当前设备）                                │
│                                                       │
│ 隐私边界                                             │
│ 患者姓名、病案号和 PDF 不进入同步数据。               │
└─────────────────────────────────────────────────────┘
```

连接后，面板变为：

```text
┌ 设备同步                                        × ┐
│ ● 已同步 · 2 台设备       最近同步 今天 14:32       │
│                                                       │
│ [立即同步]   [连接新设备]   [退出同步空间]             │
│                                                       │
│ 已连接设备                                           │
│ ● MacBook Pro（本机）                    当前设备     │
│   iPad Pro                              2 分钟前       │
│                                      [撤销访问]        │
│                                                       │
│ 本地有 1 条待同步操作                                 │
└─────────────────────────────────────────────────────┘
```

配对页只显示短期有效的一次性代码（建议 6 位数字 + 二维码），并明确提示“不要通过聊天工具转发包含同步密钥的截图”。配对成功后代码立即失效。

### 3.3 冲突处理

如果两台设备同时修改同一份记录，状态入口变为“需要处理 1 个冲突”，打开后展示：

| 项目 | 本机 | 另一设备 |
| --- | --- | --- |
| 最后修改 | 14:31 | 14:32 |
| 累计消融 | 18 | 21 |
| 已覆盖位置 | 43 | 46 |
| PVI 终点 | 2 项 | 2 项 |

操作按钮为“保留本机”“采用另一设备”“查看操作差异”。不会直接用时间戳覆盖临床记录。

## 4. 数据模型

同步对象使用独立信封，避免把服务端协议和页面内部状态耦合：

```js
{
  format: "victorypvi-sync",
  formatVersion: 1,
  spaceId: "uuid",
  recordId: "uuid",
  revision: 42,
  deviceId: "uuid",
  updatedAt: "2026-09-04T14:32:10.000Z",
  payload: {
    schemaVersion: 7,
    counts: {},
    applications: {},
    ridgeApplications: {},
    sequenceEvents: [],
    endpoints: {},
    settings: {},
    electrodeSelection: [],
    lastOperation: {}
  }
}
```

`payload` 只包含当前质控记录。以下内容明确排除：

- 患者姓名、性别、病种、手术日期、病案号和其它报告信息；
- 导出的 PDF 文件；
- 浏览器指纹、完整 IP、设备文件路径和无关操作日志。

本机额外保存：

- `deviceId` 和用户设置的设备名称；
- 待同步操作队列；
- 最近同步游标；
- 加密密钥的本机安全存储引用，不保存明文配对码。

## 5. 同步算法

### 5.1 写入顺序

1. 用户点击扇区、脊部或终点控件。
2. 当前页面立即更新并写入 `localStorage`。
3. 将本次操作追加到带唯一 `eventId` 的本地队列。
4. 若在线，Provider 尝试提交；失败则保留队列并显示待同步数量。
5. 服务端返回新 revision 后，客户端记录游标并清理已确认事件。

### 5.2 并发修改

初版采用乐观并发控制：提交时携带 `baseRevision`。如果服务端发现 revision 已变化，则返回冲突包，客户端进入冲突页面。

为了避免对临床记录进行危险的“智能猜测”，初版不自动合并两个不同设备对同一扇区的修改。后续可以在操作日志稳定后，仅对互不相同且均为追加的事件提供“建议合并”，仍需用户确认。

### 5.3 离线与恢复

- 离线时照常使用，顶部显示“仅本机保存”；
- 页面重新获得网络时尝试同步，不阻塞记录操作；
- 连续失败采用逐步延迟重试；
- 关闭同步空间不会删除本机记录，只停止上传和拉取；
- 用户可单独清除云端副本，并保留本机记录。

## 6. 加密与隐私

- 内容使用 Web Crypto API 的 AES-256-GCM 加密；
- 每个同步空间使用随机数据密钥，配对码只用于一次性传递或包裹该密钥；
- 密钥不通过普通查询参数传输，不进入分析日志；
- 服务端只能看到密文、空间 ID 的不可逆标识、revision 和时间戳；
- 同步前再次提示：不要在患者信息表中填写不应进入同步空间的数据；
- 设备撤销后拒绝后续拉取，并轮换同步密钥；
- 页面继续允许完全不连接同步服务，满足本地部署和机构离线场景。

## 7. Provider 接口

页面只依赖一个最小接口，后端实现可以替换：

```js
const syncProvider = {
  async createSpace() {},
  async pairDevice(pairingCode) {},
  async getSnapshot(spaceId, cursor) {},
  async appendEvents(spaceId, events, baseRevision) {},
  async listDevices(spaceId) {},
  async revokeDevice(spaceId, deviceId) {},
  async deleteRemoteCopy(spaceId) {},
};
```

推荐的第一种云端实现是 Cloudflare Worker + Durable Object，或者具备行级安全策略的 Supabase。无论选哪一种，客户端都必须先加密，再发送数据；不能把 Supabase/Firebase 当作明文临床数据库。

## 8. 分阶段落地

### 阶段一：静态页面可用的同步包

- 增加“设备同步”入口、状态显示和同步中心；
- 使用 Web Crypto 生成加密 `.vp-sync` 文件；
- 新设备输入口令后导入，先预览差异再确认覆盖；
- 保留当前 localStorage 行为；
- 增加导入失败、版本不兼容和本机备份提示。

### 阶段二：云端自动同步

- 已接入 Cloudflare Worker + SQLite-backed Durable Object Provider；
- 已建立同步空间、配对码、设备状态和断线重连；
- 配对完成后推送完整加密快照，并优先通过 WebSocket 广播每次状态变化；
- 镜像端自动解密并更新图形，不要求手动刷新页面；
- 重连时返回最近快照，保证镜像端恢复最新状态；
- 如果 iOS 微信内置浏览器限制 WebSocket，自动切换到 HTTPS 轮询/推送备用通道；
- Worker 已部署到目标 Cloudflare 账号；仍建议用实际 iOS 微信设备完成一次线上端到端验证。

### 阶段三：机构部署能力

- 支持机构自建 Provider 地址；
- 增加保留期限、审计与删除策略配置；
- 在部署文档中说明 CORS、HTTPS、密钥轮换和备份要求；
- 在没有配置 Provider 时隐藏云端选项，不影响本地功能。

## 9. 验收标准

- 两台不同设备可通过一次性配对完成同一记录的安全迁移；
- 配对后，操作端每次新增、撤回、清空或修改状态，镜像端无需刷新即可更新；
- 首次加入会话的设备能先获得完整状态，再接收后续增量事件；
- 断线重连后不会遗漏事件，必要时会自动重新拉取快照；
- 断网时记录、撤回、清空和 PDF 导出不受影响；
- 同步状态能明确区分“已同步、待同步、失败、冲突”；
- 患者基础信息和 PDF 在网络请求、同步包和服务端日志中均不可见；
- 并行修改不会被静默覆盖；
- 用户可以查看、撤销设备并删除云端副本；
- 未配置同步服务时，现有 GitHub Pages 版本仍可作为完整的本地应用使用。
